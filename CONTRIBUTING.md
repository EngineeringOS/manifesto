# Contributing

Start with `README.md`, then read the handbook in `docs/`.

When a change affects platform contracts, capture it in `docs/rfc/`.
When a change affects product surfaces, update `docs/prd/`.
When a change adds external standards or ecosystem method, update `docs/references/` or `docs/technologies/`.
